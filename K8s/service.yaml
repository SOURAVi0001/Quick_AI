apiVersion: v1
kind: Service
metadata:
  name: quickai-backend-service
spec:
  type: NodePort
  selector:
    app: quickai-backend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
